# 2026-07-24 导师线上走查与用户复核整改执行报告

> 历史基线：M-01—M-24、A-01—A-09、F-01—F-12 的代码、全量门禁、独立终审和真实主线均已收口；后端 467 passed、4 skipped，前端 52 files、358 tests，未闭环 P0/P1 = 0。
> 用户复核状态：U-01—U-32 **已完成本地实现与最新工作树全门禁；U-01—U-12 已完成真实浏览器分支验收；尚未部署生产**。
> 生产状态：香港生产仍为已发布基线 `57a25b7`；Actions `30139599899` 和备份 `backups/20260725_100220` 只证明 M/A/F 发布，不包含 U 批次。
> 问题依据：2026-07-24 导师线上逐项走查视频、逐字稿，以及走查后对同一数据链路的补充代码审计。
> 执行矩阵：[`../product/2026-07-24-meeting-remediation-plan.md`](../product/2026-07-24-meeting-remediation-plan.md)。

## 1. 汇报摘要

本轮不是把原字段表换一种界面照搬，而是重新按真实 CVD 录入顺序检查“字段是否适用、何时展开、如何验证、如何冻结引用、能否附证据、锁定和导出后能否还原”。共纳入：

- 导师会议明确问题 **M-01—M-24，共 24 项**；
- 会议未逐项点出、但沿同一链路发现的问题 **A-01—A-09，共 9 项**；
- 合计 **33 项**，均已落实到字段单一源、前后端契约、权限、R0、附件或导出中的相应位置，并留下自动化回归；
- M/A/F 发布后，用户按真实填写并继续终审发现 **U-01—U-32，共 32 项**，覆盖字段遗漏、归属重复、条件展开、术语、旧数据兼容和批次语义；这 32 项已在最新本地工作树完成实现和终验，但尚未发布生产；
- 另有 **11 项专业词表或参数模板**必须由导师、俊杰和实际实验人员裁定，工程端不代替课题组发明术语。

M/A/F 保持现有数据架构：复杂结构继续使用既有 `payload_json` / `attrs`，文件继续使用既有 `FileAsset`，**没有新增数据库表、没有修改已发布 Alembic 基线、没有引入新依赖**。生产 `57a25b7` 使用字段源 v3.7：84 个实验字段、44 个实体字段，其中 30 个字段标记为 R0；运行时 `check-r0` 另含跨实体和结构合规判据。U 批次的本地候选字段源为 v3.11：88 个实验字段、49 个实体字段、30 个 R0 字段标记；字段源校验和全部生成物漂移检查已通过。

完成口径严格分层：

1. **M/A/F 已实现并终验**：代码、字段源、生成物和对应回归已经落地；稳定基线后端 467 passed、4 skipped，前端 52 files、358 tests；
2. **M/A/F 本地主线已验**：一次性本地环境完成 `CVD-2026-0001`，覆盖保存、重载、锁定、样品、结果、附件、R0 与 JSON/ZIP 导出；
3. **M/A/F 生产已发布**：取得用户确认后按既有普通增量流程发布，生产健康、数据保持和真实浏览器只读复验均通过；
4. **U 本地整改已终验**：代码、生成物和全量门禁均在同一最新工作树完成，U-01—U-12 另有真实浏览器分支证据；该结论仅代表本地候选，不能解释为线上已有。

## 2. 导师会议明确问题 M-01—M-24

| 编号 | 整改结论                                                                                                                                                                       | 当前状态                                 | 代码与自动化证据                                                                                                                                         |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M-01 | 环境温度、环境湿度、实验前检查改为每条制备记录必填；编号由系统生成                                                                                                             | 已实现；浏览器已验                       | `field-source.yaml`、`v2_experiment_service.py`、`basic-info-section.tsx`；API 新建/锁定与前端基本信息回归                                               |
| M-02 | 目标形态进入必填门，目标性能继续选填                                                                                                                                           | 已实现；浏览器已验                       | 字段源、生成模型、`v2_r0_service.py`、`target-product-section.tsx`；R0 与字段逻辑测试                                                                    |
| M-03 | 化学式规则贴近输入，前后端均接受常见括号、结晶水等组成写法；组成明细仍是复合体系权威                                                                                           | 已实现                                   | `formula.ts`、`v2_entity_service.py`；`formula.test.ts` 与 `test_material_lot_formula_accepts_common_grouped_and_hydrated_formulas`                      |
| M-04 | 删除不适用的装置照搬字段；坐标系固定只读；温区传感器与不确定度进入装置版本                                                                                                     | 已实现；浏览器已验                       | `field-source.yaml`、`temperature-sensors-editor.tsx`、实体强类型模型；`test_setup_requires_sensor_contract_and_rejects_removed_reference_fields`        |
| M-05 | 管材质与管形状分别录入，不把“石英/刚玉”“圆/方/矩”当成带斜杠的一个值                                                                                                            | 已实现；浏览器已验                       | 字段源、实体表单与双语选项；实体表单和硬编码文案回归                                                                                                     |
| M-06 | 物料批次支持瓶身/包装标签附件；前驱体和衬底必须引用匹配类别的冻结批次                                                                                                          | 批次引用浏览器已验；标签附件自动化已验   | `entity_files.py`、`entity-reference-select.tsx`、`v2_entity_snapshot_service.py`；实体附件与批次冻结 API 回归                                           |
| M-07 | 前驱体处理改为有序多步骤；旋涂、熔融、压片、退火、研磨等按方法展开参数，“其他”必须具名并填写参数                                                                               | 直接装载浏览器已验；方法展开自动化已验   | `treatment-steps-editor.tsx`；已知方法、其他方法、等离子参数的组件回归                                                                                   |
| M-08 | 衬底化学式、晶向、偏转角、粗糙度类型/数值、尺寸、放置方式拆分；倾斜与预处理按条件展开                                                                                          | 已实现；浏览器已验                       | `repeatable-items-section.tsx`、`process-detail-editors.tsx`、字段逻辑；结构回读与条件显隐回归                                                           |
| M-09 | 原 11 个过程阶段收敛为“预处理”“反应条件”“其他记录”三类；温度、气体、压力、时长、降温和实际外场在正确记录中出现                                                                 | 已实现；浏览器已验                       | `process-steps-section.tsx`、`process-steps-logic.test.ts`、生成模型；测试锁定恰好三种记录类型                                                           |
| M-10 | 具名“其他记录”成为正式可保存结构；旧阶段明确提示删除或重录，不再假装保存后静默丢失                                                                                             | 已实现；自动化已验                       | `process-steps-logic.ts`、`process-steps-section.tsx`、`v2_process_semantics.py`；退役阶段识别与 payload 回读回归                                        |
| M-11 | 温度程序改为按温区的时间—设定温度点；全温区覆盖、编号唯一、时间严格递增                                                                                                        | 已实现；浏览器已验                       | `temperature-program-editor.tsx`、`v2_process_semantics.py`；组件边界测试与 `test_setup_sensors_and_temperature_program_cover_every_zone`                |
| M-12 | 实测温度文件真正接入既有附件系统，并记录时间列、温区通道和过程记录绑定                                                                                                         | 已实现；浏览器已验                       | `MeasuredTemperature` 编辑器、`file_asset_service.py`；文件角色/同炉次校验与通道重复、越界回归                                                           |
| M-13 | 每种气体独立引用气瓶批次、冻结纯度和记录多个供气区间，可表达停气与二次供气                                                                                                     | 已实现；浏览器已验                       | `gas-feeds-editor.tsx`、`v2_process_semantics.py`；多区间回读、身份切换清理、重叠拦截和冻结快照测试                                                      |
| M-14 | Setup 只声明外场能力；本条制备记录填写实际启用类型、时间区间和具名参数，超能力或超时长拒绝                                                                                     | 已实现；浏览器已验                       | `FieldParamsEditor`、字段逻辑、`v2_process_semantics.py`；能力越界与时间上界回归                                                                         |
| M-15 | 过程事件拆分为事件、是否终止、终止原因、具体说明和处理措施；每条事件使用稳定 UUID 绑定附件                                                                                     | 已实现；浏览器已验                       | `repeatable-items-section.tsx`、`experiment-attachments.tsx`、文件服务；事件附件绑定与移除清理回归                                                       |
| M-16 | 直接观察支持图片/文件；空记录、空 `attrs` 或无明细的“其他”不能清除“结果待补”                                                                                                   | 已实现；浏览器已验                       | `results-section.tsx`、`v2_result_evidence.py`；统一结果 API、附件与 `test_other_phenomenon_needs_detail_to_clear_missing_flag`                          |
| M-17 | MaterialLot、Setup、Instrument 仅管理员可创建、追加版本和维护附件；成员只读、引用和下载                                                                                        | 管理员创建浏览器已验；权限矩阵自动化已验 | `v2.py`、`entity_files.py`、`experiment_guards.py`；管理员/成员权限与未绑定文件访问回归                                                                  |
| M-18 | Setup 冻结快照是设备投影唯一权威；通用模块不能覆盖设备信息                                                                                                                     | 已实现                                   | `v2_entity_snapshot_service.py`、`equipment-section.tsx`、`v2_r0_service.py`；冲突 payload 与 Setup 引用保存回归                                         |
| M-19 | 三类实体载荷改为强类型；未知字段和条件不适用字段在 API 边界拒绝                                                                                                                | 已实现                                   | `schemas/v2.py`、生成模型、`v2_entity_service.py`；OpenAPI/422、实体日期、数值与删除字段回归                                                             |
| M-20 | R0 不再只检查非空，而是复验结构，并要求温度、气体、压力等来自同一条适用反应记录                                                                                                | 已实现                                   | `v2_r0_service.py`、`v2_process_semantics.py`；`test_r0_reports_exact_preparation_and_reaction_counts` 及锁定/R0 回归                                    |
| M-21 | 物料下拉按类别/身份筛选，仪器按表征方法筛选；选择后显示实际冻结版本，切换条件清理不兼容引用                                                                                    | 已实现；浏览器已验                       | `entity-reference-select.tsx`、`reference-snapshot.ts`、`results-section.tsx`；引用筛选、旧版本显示和不兼容仪器清理回归                                  |
| M-22 | 字段字典补齐输入/机器类型、说明、示例和 schema 路径；ZIP 带 JSON Schema；`files.csv` 提供稳定文件键和绑定键                                                                    | 已实现；导出已验                         | `export_v2_schema.py`、`v2_reporting_service.py`、生成字段字典；`test_zip_export_includes_reversible_remaining_modules_and_schema_metadata`              |
| M-23 | 删除章节标题下灰色说明、“可重复条目”等机器化噪音和无意义斜杠；中英文主称呼统一；制备实验记录与表征实验记录成为并列一级导航，表征列表汇总现有统一结果并可进入对应制备记录或样品 | 已实现；浏览器双语已验                   | `app-shell.tsx`、`module-card.tsx`、`characterization-list-page.tsx`、zh/en locale；主导航、统一结果聚合、列表交互、CJK 硬编码、字段 i18n 和结构标签回归 |
| M-24 | STATUS 不再以历史加固结果代替本轮状态，明确字段数、自动化与本地主线证据、专业待定项和生产发布状态                                                                              | 已实现；最终数已回填                     | [`../standard/STATUS.md`](../standard/STATUS.md)、本报告与整改计划                                                                                       |

## 3. 会外代码审计问题 A-01—A-09

| 编号 | 整改结论                                                                                   | 当前状态                               | 代码与自动化证据                                                                                     |
| ---- | ------------------------------------------------------------------------------------------ | -------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| A-01 | 气体种类、气瓶身份、冻结版本和纯度快照前后端双重校验；切换种类即清除不兼容批次             | 已实现；浏览器已验                     | `gas-feeds-editor.tsx`、`v2_process_semantics.py`；受控/其他气体身份正反测试                         |
| A-02 | Setup 传感器与设定温度均须恰好覆盖 1…N 温区，不能缺失、重复或越界                          | 已实现                                 | `temperature-sensors-editor.tsx`、`temperature-program-editor.tsx`、共享过程语义；全覆盖正反测试     |
| A-03 | 供气、外场等时间区间必须满足起止关系，且不能超过反应总时长；保存与 R0 双检                 | 已实现                                 | `v2_process_semantics.py` 与前端结构编辑器；`test_process_timed_values_cannot_exceed_total_duration` |
| A-04 | “其他方法”“其他观察现象”增加具名明细并条件必填；空 other 不作为结果证据                    | 已实现；浏览器已验                     | `schemas/v2.py`、`results-section.tsx`、结果证据服务；统一结果和结果待补回归                         |
| A-05 | 仪器按方法和具名其他方法筛选；改变方法立即清除不兼容仪器                                   | 已实现；浏览器已验                     | `results-section.tsx`；仪器过滤组件回归与后端方法—仪器类型匹配测试                                   |
| A-06 | `files.csv` 增加 `file_id`、`binding_type`、`binding_id`，事件、实测温度和结果附件均可回链 | 已实现；导出已验                       | `v2_reporting_service.py`；`test_files_csv_exposes_joinable_file_and_binding_ids`                    |
| A-07 | 保存过程时事务性软删除已移除的事件/温度绑定；前端清理本次上传但尚未保存的临时附件          | 已实现；接口与导出已验                 | `file_asset_service.py`、`experiment-attachments.tsx`；保存移除、取消和卸载生命周期回归              |
| A-08 | 引用显示实际冻结版本而非最新版本；实体历史中的非空“其他”值可回到同类下拉继续选择           | 冻结版本浏览器已验；历史选项自动化已验 | `v2_entity_snapshot_service.py`、`reference-snapshot.ts`、实体/引用组件；冻结版本与历史选项回归      |
| A-09 | 旧过程阶段无法可靠映射时明确要求删除或重录；结果附件为 `null` 时按空数组处理，不再崩溃     | 已实现；自动化已验                     | 退役阶段提示、`results-section.tsx`；旧阶段识别与 null 附件回归                                      |

## 4. 最终收口审计 F-01—F-12

在 M/A 矩阵完成后，针对“仍像表格照搬、字段不分情况、功能入口缺失、机器值外露”的风险又做了一轮独立终审。F 项包含对既有问题的复验和新增缺口，不与 33 项简单相加。

| 编号 | 最终审计发现                                                 | 收口结果                                                                                             |
| ---- | ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- |
| F-01 | 表征记录缺少独立一级入口，未有结果的样品也无法进入表征工作流 | 已增加“制备实验记录 / 表征实验记录”并列导航和独立汇总列表；浏览器验收显示 1 个样品、2 条记录并可回链 |
| F-02 | 气体前驱体、气瓶批次的类别与相态契约不一致                   | 实体类别、相态、气体身份、气瓶批次及纯度快照已统一，错误组合前后端均拒绝                             |
| F-03 | 蓝宝石 canonical 值与自定义值不一致                          | 字段源、实体输入与显示值统一；浏览器用 `SAPPHIRE-E2E-01` 完成冻结引用                                |
| F-04 | API 仍可能接受实体类别不适用字段                             | 强类型入口在边界拒绝未知字段和反向不适用字段，不能把斜杠或空字段照搬入库                             |
| F-05 | 表征结果不按方法展开、暴露机器指标码、缺仪器冻结快照         | Raman 等方法使用受控条件与结果参数；浏览器显示人读名称、固定单位及仪器 v1 冻结快照                   |
| F-06 | 掺杂体系错误强制 `dopant:matrix`，不能表达共掺杂             | 组成明细支持多个具名角色；不再用单一字符串假设覆盖真实体系                                           |
| F-07 | 实测温度上传未真实解析 CSV/XLSX 与数值列                     | 上传时解析表头、数值列、有效列对和行数；本地 CSV 得到 3 个数值列、3 组有效列对和 7 行                |
| F-08 | 样品详情泄露 UUID、snapshot、attrs 等机器键                  | 页面改为来源、冻结批次、衬底条件和文件等人读信息；UUID 只保留在路由/关系键中                         |
| F-09 | 章节标题下仍显示灰色模板说明和机器化提示                     | 已删除模板噪音，仅保留具体填写规则、必填原因与操作反馈                                               |
| F-10 | 终止原因选择“其他”时未强制具体说明                           | 前后端条件必填一致；空“其他”不能保存或通过 R0                                                        |
| F-11 | 文档在最终门禁和浏览器之前提前宣称全部完成                   | STATUS、计划和本报告改为分层状态，并在本节回填最终数字和真实主线证据                                 |
| F-12 | 复合下拉对象进入通用校验导致实体创建 500                     | 在共享实体服务根因修复；浏览器重新创建蓝宝石衬底批次成功，相关 API 回归覆盖                          |

最终独立复核：**未闭环 P0/P1 = 0**。

## 5. 发布后用户复核与终审 U-01—U-32

本节记录确定的整改口径与最新工作树证据。所有条目统一边界为：**本地实现和终验已完成；未部署生产**。

| 编号 | 复核结论与本地整改口径                                                                                                                  | 当前状态                  | 终验证据                                                                                                              |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| U-01 | 回查逐字稿后确认老师在 00:05:14–00:07:14 建议删掉悬浮粒子浓度；已从基本信息、创建载荷、后端契约和字段源删除，温湿度与预检保留为每炉必填 | 本地完成；页面已验        | 页面、OpenAPI、JSON Schema、字段字典、xlsx 与导出均无粒子字段；真实新建页仅保留温湿度和预检                           |
| U-02 | 炉管履历改为每炉手填；非气态前驱体的舟/坩埚按每个前驱体分别填写重置次数和本次使用序号                                                   | 本地完成；自动化/页面已验 | 页面保存炉管 `0/1`；固态前驱体展开独立舟/坩埚履历；多条目、边界和回读有前后端回归                                     |
| U-03 | 炉管尺寸按截面展开：圆形=外径+壁厚，方形=外边长+壁厚，矩形=外宽+外高+壁厚，自定义=具名尺寸说明                                          | 本地完成；自动化/页面已验 | 四类截面切换、清理和契约回归通过；真实矩形 Setup 保存并回读为外宽 80、外高 60、壁厚 2                                 |
| U-04 | 截图中的保存失败来自两个传感器都指向温区 1；Setup 按温区数固定生成 1…N 传感器，温度程序同样固定覆盖，其他位置字段改用有效温区选择       | 本地完成；自动化/页面已验 | 两温区 Setup 以温区 1/2 各一传感器保存；前驱体与衬底只显示有效温区选项，衬底温区 2、距离 -5 mm 保存成功               |
| U-05 | 用户可见术语统一为“等离子体”，预处理显示“等离子体处理”；旧“等离子”仅保留为兼容输入别名                                                  | 本地完成；自动化/页面已验 | 冻结装置快照显示“等离子体”；字段级旧别名兼容回归通过                                                                  |
| U-06 | 体相空间群使用内置 230 项本地搜索与符号显示，删除对外部网站操作说明的依赖，并缩短帮助文字                                               | 本地完成；自动化/页面已验 | 真实表单输入 `194` 即显示 `P6₃/mmc`；1–230、越界和留空回归通过                                                        |
| U-07 | “所在温区与源温”改为“前驱体位置”：温区是主信息，局部温度仅在另有实测/估计或不同于温区设定时填写，并要求温度依据                         | 本地完成；自动化/页面已验 | 真实表单显示温区、可选温度和实测/估计依据；三条正向路径与缺依据反例回归通过                                           |
| U-08 | “偏转角”明确为相对标称晶面的“偏切角”；正角度必须同时填写偏切方向，例如“c 面向 a 轴偏 0.2°”                                              | 本地完成；自动化/页面已验 | MaterialLot 以 0.2° 和“c 面向 a 轴”锁版；炉次只读回读；0°/正角度缺方向正反测试通过                                    |
| U-09 | 稳定事实归 MaterialLot：材料身份、晶向/抛光、偏切、氧化层和标称粗糙度随版本冻结；炉次只填本片几何/放置、处理、位置及使用履历            | 本地完成；自动化/页面已验 | 稳定字段只读投影；本片 10×10×0.5、正放、温区 2/-5 mm 保存；客户端冲突被覆盖；不完整旧版本被拒并提供显式“使用最新版本” |
| U-10 | Setup 只声明外场能力，炉次按等离子体/光/电分别展开实际参数；附加参数仍要求名称、数值和单位                                              | 本地完成；自动化已验      | 三类参数契约、能力/时长越界、类型切换清理、保存回读和后端锁定回归通过                                                 |
| U-11 | “表征方法”与仪器引用拆清；Instrument 固定配置仅记录稳定硬件/能力，单次波长、功率、物镜、积分时间等进入结果测试条件                      | 本地完成；自动化已验      | 方法必填、仪器筛选、固定配置与单次条件归属、历史版本回读继续由 M/F 回归覆盖                                           |
| U-12 | 气体数值纯度百分比作为权威值；供应商 6N/5N 等等级仅作为原标签选填，不再形成两项强制重复事实                                             | 本地完成；自动化已验      | 仅数值纯度可完成；等级不能替代权威值；实体快照、过程引用和导出回归通过                                                |
| U-13 | 锁定前在副本上按当前契约重验全部已存模块；物料引用执行补齐权威投影→验证→重新冻结→二次验证，不修改旧草稿                                 | 本地完成；自动化已验      | 旧完整 lot 缺 availability 可兼容；旧坏舟/温度/偏切/批次被具名 422；失败前后存量不变                                 |
| U-14 | 历史 Setup 引用前复验形状—尺寸和 1…N 传感器覆盖；验证失败时不写 Setup 引用或半有效 equipment                                           | 本地完成；自动化已验      | 坏历史 Setup 422 且引用保持不变；合规 Setup 切换与锁定温区依赖回归通过                                                |
| U-15 | 填写距热电偶距离时，“前驱体位置”动态必填；后端同样要求所在温区，距离不再允许失去参照                                                    | 本地完成；自动化已验      | 距离+空温区前后端失败；选择有效温区后保存通过                                                                          |
| U-16 | 所有带本地条件的字段统一拒绝不适用残留；粒径仅适用于化学品，形态不适用于气瓶，前驱体外观仅适用于固态                                    | 本地完成；自动化已验      | 气瓶粒径/粉末形态和气态前驱体粉末外观 API 反例均失败；前端类别切换显隐通过                                             |
| U-17 | 晶向/抛光和偏切各自使用“有规格数值 / 供应商未提供 / 不适用”；只有有规格时展开明细，0 不再代表未知                                      | 本地完成；自动化已验      | Cu 箔不适用路径成功；蓝宝石有规格路径、正偏切方向和不适用字段残留均有正反测试                                         |
| U-18 | 已知衬底材料与化学式在 MaterialLot 创建/追加版本边界即时校验；前端把不一致错误显示在化学式字段，自定义材料仍可自由填写                  | 本地完成；自动化已验      | 蓝宝石+MoS₂ 前后端拒绝；蓝宝石+Al₂O₃、Cu 箔+Cu 与自定义材料正向通过                                                   |
| U-19 | 旧预处理 `type="等离子"` 使用字段专用别名归一为 `plasma_treatment`，避免被装置外场的全局 `plasma` 码串扰                              | 本地完成；自动化已验      | 后端判别联合和前端旧载荷回读均通过；新显示仍为“等离子体处理”                                                          |
| U-20 | 隐藏条件字段不再参与前端校验，残留旧值也不会形成不可见错误或进入提交载荷                                                              | 本地完成；自动化已验      | 隐藏非法偏切角回归可保存且载荷无该字段；同一共用入口覆盖类别/状态切换                                                 |
| U-21 | 锁定、R0、required 与报告统一使用标准化副本/冻结批次权威投影，兼容补齐不写回旧草稿                                                     | 本地完成；自动化已验      | 完整 APCVD 旧衬底缺 availability 仍可锁定、报告 compliant、数据库载荷保持不变                                         |
| U-22 | 正常炉次可以没有过程事件；仅真实存在的事件才要求事件 ID、类型、时间、终止状态及附件语义                                                | 本地完成；自动化已验      | 完整 APCVD 无 `process_events` 锁定成功；事件字段与附件反例继续通过                                                    |
| U-23 | 前驱体/衬底页面显示、缺失检查和 payload 构建共用 MaterialLot 权威投影，消除 raw/display 分叉                                            | 本地完成；自动化已验      | 旧衬底状态由快照推断后可分节保存；伪造的炉次材料与化学式被权威快照覆盖                                                |
| U-24 | 0° 偏切时前端隐藏并剔除方向，后端同时拒绝“0° + 非空方向”的矛盾稳定事实                                                                 | 本地完成；自动化已验      | MaterialLot/炉次两套显隐、提交与后端生成模型正反回归通过                                                             |
| U-25 | “使用最新版本”升级入口执行与下拉相同的类别/完整性筛选，不再把旧引用升级到不兼容最新版本                                                | 本地完成；自动化已验      | filter=false 时无升级按钮，合规最新版本仍可显式切换                                                                  |
| U-26 | 旧温区自由文本先经生成模型复验；后续温区/外场检查只读取已验证结构，锁定不再因 `.get` 返回 500                                           | 本地完成；自动化已验      | 旧坏温区值具名 422 且载荷不改写；合法温区与装置能力边界继续覆盖                                                       |
| U-27 | 事件说明始终可见且可选，仅在事件确实终止实验且终止原因=其他时必填；隐藏的旧终止原因不再驱动校验                                       | 本地完成；自动化已验      | 取消终止后 stale `other` 不阻断保存；真实“其他”原因缺说明仍被当前字段拦截                                             |
| U-28 | Instrument 原“仪器名称与类型”改称“适用表征方法”，编号、厂商与型号继续承担仪器身份                                                      | 本地完成；生成物已验      | 中英前端元数据、字段字典和 xlsx 一致                                                                                  |
| U-29 | 样品快照、R0、报告、单炉 JSON 和关系型 CSV 统一从冻结 MaterialLot 快照重建批次稳定事实                                                   | 本地完成；自动化已验      | 旧衬底原载荷缺状态仍可锁定且 JSON/CSV 输出完整；样品不保留被批次省略的旧值                                           |
| U-30 | 切换物理 Setup 时清空上一台装置的本炉炉管履历，同一 Setup 内编辑不误清                                                                  | 本地完成；自动化已验      | 切换后未重填设备分节被就地拦截，重填后引用新装置                                                                     |
| U-31 | 前驱体/衬底批次稳定字段选 lot 前即只读并提示真实职责；服务端保存时先清稳定字段再覆盖冻结快照                                              | 本地完成；自动化已验      | 用户不再重复填写名称/化学式/晶向/偏切；伪造稳定字段不落库，本炉尺寸/处理/位置仍可填                                  |
| U-32 | 选择炉管截面后，尺寸动态必填并按圆形、方形、矩形或其他截面展开；缺失在当前字段给出专用说明                                                | 本地完成；自动化已验      | “请按所选截面完整填写炉管尺寸”出现时不发提交；完整结构通过前后端契约                                                  |

U 批次最新工作树终验：后端 Ruff 与 format 通过，`pytest` **497 passed、4 skipped**；前端 Prettier、ESLint、TypeScript 与生产构建通过，`vitest` **52 files、392 tests**；字段源 v3.11 校验为 **88 个实验字段、49 个实体字段、30 个 R0 字段标记**，Pydantic、JSON Schema、OpenAPI、前端元数据、字段字典与 xlsx 一致，`git diff --check` 通过。一次性本地数据库真实页面已创建并保存两温区矩形 Setup、蓝宝石 MaterialLot 和制备记录分节，验证 U-01—U-12 的无粒子字段、温区 1/2、矩形尺寸、等离子体、空间群 194、批次只读事实、偏切方向、本片尺寸与温区距离；最终未保存修改为 0，浏览器 console warning/error 为 0。U-13—U-32 的旧草稿、历史 Setup、不适用分支、字段所有权、冲突批次、无事件和旧投影路径由全量自动化覆盖。

## 6. M/A/F 历史自动化门禁

以下数字来自 M/A/F 发布前稳定工作树的全量复跑，不使用单个定向用例代替；**它们不是 U 批次的终验数字**：

| 门禁                                | 最终结果                                                                                                          |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 后端 `uv run ruff check .`          | 通过                                                                                                              |
| 后端 `uv run ruff format --check .` | 通过，117 files already formatted                                                                                 |
| 后端 `uv run pytest -q`             | 467 passed，4 skipped                                                                                             |
| 前端 `bun run check` / Prettier     | 通过                                                                                                              |
| 前端 `bun run lint`                 | 通过，0 warnings                                                                                                  |
| 前端 `bun run typecheck`            | 通过                                                                                                              |
| 前端 `bun run test`                 | 52 files、358 tests 通过                                                                                          |
| 前端 `bun run build`                | 通过，2229 modules transformed                                                                                    |
| 字段源与生成物漂移检查              | 84 个实验字段、44 个实体字段、30 个 R0 字段标记；Pydantic、JSON Schema、OpenAPI、前端元数据、字段字典和 xlsx 一致 |
| `git diff --check`                  | 通过                                                                                                              |

前端命令仅有 Node `DEP0205 module.register()` 弃用提示，不影响检查、测试或构建结果。

U 批次的新门禁数字与浏览器分支证据见 §5；本节继续只保留 M/A/F 历史基线，避免混用。

## 7. M/A/F 历史本地真实主线验收

> 验收日期：2026-07-24。使用一次性本地 SQLite 与文件目录，前端和后端均指向本地；未连接香港生产。页面交互通过本地浏览器完成；由于宿主浏览器拒绝把本机临时文件交给文件选择器，文件字节通过系统同一公开鉴权 API 上传，再由模块保存、导出与 R0 反向核对。

| 验收项                                                                 | 状态                   | 最终证据                                                                                                                                                                                                            |
| ---------------------------------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 管理员新建化学前驱体、衬底、Ar/H₂ 气瓶批次、两温区 Setup 和 Raman 仪器 | 通过                   | `MO3-E2E-01`、`SAPPHIRE-E2E-01`、`AR-E2E-01`、`H2-E2E-01`、`SETUP-E2E-02Z`、`RAMAN-E2E-01`                                                                                                                          |
| 新建制备记录，填写环境温湿度与预检、目标产物和冻结引用                 | 通过                   | 制备记录 `CVD-2026-0001`；24.5 ℃、45 %RH、预检确认、MoS2、本征、1 层、空间群 194                                                                                                                                    |
| 倾斜衬底及角度、粗糙度、预处理条件展开                                 | 通过                   | 蓝宝石、偏转角 0°、RMS 0.2 nm、10×10×0.5 mm、倾斜 30°、丙酮清洗 10 min、等待 5 min、温区 2、距离 -5 mm                                                                                                              |
| 两温区温度、两种气体、二次供气、压力、时长、降温和实际外场             | 通过                   | 两区首点均 0 min；Ar 0–30/45–50 min，H₂ 0–30 min；占比 90.91%/9.09%；101325 Pa、60 min、派生循环 2、随炉冷却 20 ℃/min、光 50 W                                                                                      |
| 过程事件稳定 ID、事件附件、实测温度文件和通道映射                      | 通过                   | 事件 UUID 与附件绑定一致；`temperature.csv` 解析为 `elapsed_s`、`zone_1_c`、`zone_2_c`，7 行并绑定反应条件                                                                                                          |
| 锁定、样品生成和 R0                                                    | 通过                   | 锁定审计写入；自动生成 `CVD-2026-0001-S01`；最终 R0 `compliant`，44 项运行时合规判据中 43 项适用且 43/43 通过                                                                                                       |
| 直接观察与仪器表征                                                     | 通过                   | “其他 + 具体说明 + 证据文件”；Raman 532 nm、1 mW、50×、10 s、3 次，2H-MoS2、1 层、E′ 峰 384.2 cm⁻¹，仪器快照和原始谱文件已关联                                                                                      |
| 独立表征列表和样品详情                                                 | 通过                   | 表征页显示 1 个样品、2 条记录和 Raman/直接观察；样品详情只显示人读来源、谱系、冻结批次与衬底条件                                                                                                                    |
| JSON 与 CSV ZIP 导出                                                   | 通过                   | `CVD-2026-0001-final.json` 与 `cvd-runs-final.zip` 校验通过；ZIP 14 项，`gas_flow_shares.csv`、`files.csv`、schema、manifest 和绑定键可重建关系                                                                     |
| 中文/英文切换                                                          | 通过                   | 主导航、样品详情、状态、单位与条件说明完成中文→英文→中文切换                                                                                                                                                        |
| 浏览器错误与意外 5xx                                                   | 通过（环境例外已记录） | 验收曾捕获 F-12 实体创建 500 并完成根因修复；本地 SQLite 开发连接出现一次只读连接，改用稳定本地服务后最终上传、保存、重锁、导出和 R0 全通过；宿主扩展弹层阻止了最后一次 console dump，不影响已完成的 UI 与 API 证据 |

最终导出确认了四类文件角色均可回链：`temperature_timeseries`、`process_event_attachment`、`direct_observation_file`、`characterization_file`；实测温度通道、事件附件、直接观察证据和 Raman 原始谱均进入同一炉次 JSON/ZIP。

该主线证明 `57a25b7` 之前的 M/A/F 基线。U 批次的两温区、形状条件尺寸、履历、批次归属和空间群等真实页面分支已另行复验；三类外场、锁定和导出由最新全量服务/API 回归覆盖。

## 8. 需要导师/组内裁定的 11 个专业问题

这些问题不应由工程人员猜答案，也不应以自由文本空壳掩盖。当前实现优先保存原始事实，并为“其他”要求明确名称或参数。

| 编号 | 请裁定的问题                                                                          | 当前安全处理                                                                                           |
| ---- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| P-01 | “观察到的现象”词表应细到什么粒度，哪些属于组内稳定选项                                | 现有客观词表继续使用；未覆盖现象选择“其他”并强制填写具体描述                                           |
| P-02 | SEM 中“覆盖率”的组内标准叫法与量化方式                                                | 不自行把主观等级冒充定量值；待确认名称、单位/区间和测量方法后进入字段源                                |
| P-03 | 堆垛类型需要区分到什么粒度                                                            | 先保存可确认的结构角色、层序和具名事实，不扩造未经确认的枚举                                           |
| P-04 | 前驱体“外观”应采用哪些稳定选项                                                        | 现有词表外内容具名保存；待实际实验人汇总高频值后再冻结                                                 |
| P-05 | 实验前气体操作统一称为“气路置换”“洗气”还是“吹扫”                                      | 当前结构已保存循环次数、气体、批次与流量；名称待组内统一                                               |
| P-06 | 低压 CVD 是否每条制备记录都要记录漏率，以及它属于装置校验还是实验过程                 | 当前不虚构必填位置；确认归属、单位和测量时点后再加入单一源                                             |
| P-07 | 多气体比例采用什么组内标准名称                                                        | 系统先按同时间段各气体流量派生，不重复存一份可能不一致的比例                                           |
| P-08 | 掺杂体系组成角色最终采用“本征材料”“基体”还是“主体”，复杂官能团化学式用什么组内示例    | 权威事实保存在组成明细；术语和示例待裁定后统一替换                                                     |
| P-09 | 压片等前驱体处理方式的最小参数模板是什么                                              | 未确认参数使用具名“参数—值—单位”，不接受无单位、无名称的自由文本                                       |
| P-10 | 低压系统是否需要记录真空泵类型、功率和转速，哪些属于装置版本、哪些属于每炉条件        | 当前只把确定的绝对工作压力放入炉次；设备能力不从字段表猜写，待确认归属后进入 Setup 或过程结构          |
| P-11 | 物料是否必须做真实瓶装试录；目录号是否必填；CoA 与瓶身/包装标签照片的证据边界如何划分 | 供应商、目录号保持可选；CoA 与标签附件分角色保存；气瓶编号、纯度和等级已具名记录，待组内确定最小证据集 |

老师确认后，统一修改 `docs/standard/field-source.yaml`，再重跑后端模型、JSON Schema、OpenAPI、前端元数据、字段字典和 xlsx；不直接手改生成物。

## 9. 发布边界与汇报结论

- 开发分支 `codex/meeting-remediation-20260724` 已经 PR #1 合并，香港生产当前应用为 `57a25b7`；该提交只代表 M/A/F 已发布基线。
- M/A/F 未新增迁移、未修改已发布基线 `20260711_0001`；其生产已按普通增量流程从 `a592116` 前滚到 `57a25b7`，发布前备份为 `backups/20260725_100220`，两条既有草稿状态未变。
- 33 项会议/会外整改的代码、自动化和历史主线已收口，最终审计未闭环 P0/P1 = 0；U-01—U-32 是发布后的新复核批次，不篡改这项历史结论。
- U 批次的本地实现、全门禁与真实浏览器分支验收已完成；**U 批次尚未部署生产，生产仍不含这些改动**。
- 发布时仍须按既有备份、迁移前检查、健康检查和回退流程取得单独发布结论；在此之前不得把本地候选字段源 v3.11 写成线上现状。
- 11 项专业裁定与 U 批次并行存在；专业问题不能由工程端擅自定标准，确定性 UX/数据归属问题也不能被推迟为专业裁定。
- 实验模板/复制、PVD、分析仪表盘、批量导入等已明确延期能力不混入本轮缺陷整改，也不用于冲淡本轮 33 项问题。
