# 2026-07-24 导师走查整改计划

> 状态：本地工程整改、最终全门禁、独立终审、真实主线和香港生产发布均已完成；当前应用提交为 `57a25b7`
> 最高优先级依据：2026-07-24 导师线上逐项走查视频与逐字稿
> 目标：在向大导汇报前，把仓库内能够确定的问题修到可真实录入、可锁定、可导出、可追溯；不以空字符串、自由文本或“保存成功但丢字段”换取假验收。

## 1. 整改边界

- 本轮直接修改字段单一源、后端契约、前端表单、R0、附件与导出，不修改已发布的 Alembic 基线。
- 复杂数据继续使用现有 `payload_json` / `attrs`，附件继续使用现有 `FileAsset`；不为赶汇报新增数据库表。
- 会议明确的删改项直接执行。会议中明确要求再向俊杰确认的专业名称，不自行发明标准术语：先采用不丢信息的具名结构与“其他 + 明确名称/参数”，并列入汇报待确认清单。
- 实施阶段先完成本地代码、测试、浏览器验收和部署前检查；生产发布经用户单独确认后执行，现已完成。

## 2. 必须关闭的问题

| 编号 | 问题                                                                       | 整改结果                                                                                                                                              | 验收证据                                             |
| ---- | -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| M-01 | 环境温度、湿度和实验前检查不是必填                                         | 三者改为必填；环境值每炉次重新录入；预检保存为明确勾选结果                                                                                            | 缺任一项不能锁定                                     |
| M-02 | 目标形态只是推荐项                                                         | 改为必填；目标性能保持选填                                                                                                                            | R0/必填门与页面一致                                  |
| M-03 | 化学式提示离输入处太远、括号被拒                                           | 规则紧邻输入；允许常见括号与组成写法；复合体系仍以组成明细为权威                                                                                      | 前后端同一校验用例                                   |
| M-04 | 装置字段照搬且互相矛盾                                                     | 删除 sccm 参考值、加热区长度、泵型号/极限压力、MFC 配置、PID；坐标系固定只读；温度传感器/不确定度进入装置版本                                         | 新装置表单不再出现被删除项；快照可带出传感器         |
| M-05 | 管材质/形状的斜杠与分隔符被当作选项                                        | 材质和形状分别录入，不再猜分隔符                                                                                                                      | 石英/刚玉与圆/方/矩独立回显                          |
| M-06 | 物料标签照片缺失，批次引用非必填                                           | MaterialLot 增加瓶身/包装标签附件；前驱体和衬底必须引用批次                                                                                           | 错批次类别拒绝；附件可预览/下载                      |
| M-07 | 前驱体处理方式只保存名称，不展开参数                                       | 改为可排序多步骤；旋涂、熔融、压片、退火、研磨等显示各自参数；“其他”必须写名称和参数                                                                  | 保存、刷新、再次编辑不丢步骤                         |
| M-08 | 衬底关键参数缺失或揉成字符串                                               | 化学式、晶向、偏转角、粗糙度类型和值、尺寸、放置方式拆开；倾斜时角度必填；预处理改多步骤                                                              | 每个条件分支有正反用例                               |
| M-09 | 11 个过程阶段重复、复杂且不符合实际                                        | 收敛为“预处理”和“反应条件”两类有序记录；预处理包含抽气/气路置换；反应条件统一记录温度、气体、压力、时长、降温和实际外场；保留明确命名的“其他记录”兜底 | 不再出现升温/稳定/生长/后退火等重复卡片              |
| M-10 | 未知“其他阶段”提示保存成功但随后消失                                       | 后端正式支持具名“其他记录”，或在边界明确拒绝；绝不静默删除                                                                                            | 回归测试证明保存后刷新仍存在                         |
| M-11 | 温度程序声明为数组却实际接受任意字符串                                     | 改为按温区的时间—设定温度点；时间严格递增、温区不能越界；保温由重复温度点表达                                                                         | `"x"` 无法通过保存/R0                                |
| M-12 | 实测温度“导入文件”没有实现                                                 | 使用现有附件系统上传 CSV/XLSX，记录时间列与温区通道映射                                                                                               | 文件与炉次、过程记录、SHA 可重建关联                 |
| M-13 | 多种气体共享一套纯度和流量                                                 | 每种气体独立记录气瓶批次、纯度快照、测量来源和多个供气区间；可表达开始、断气、再次供气；同时间段体积流量占比可派生                                    | Ar/H₂ 参数互不串值；二次供气可保存                   |
| M-14 | 外场能力被当作本炉实际使用                                                 | Setup 只记录能力；炉次记录实际启用的外场、时间区间和具名参数                                                                                          | 无外场不展开；超出装置能力拒绝                       |
| M-15 | 异常事件字段不并列且没有附件                                               | 事件、是否终止、终止原因、具体说明、处理措施拆开；每条事件有稳定 ID 和附件                                                                            | 附件与具体事件关联，刷新不丢                         |
| M-16 | 直接观察没有图片/文件证据                                                  | 直接观察支持附件；空记录和任意 `attrs` 不再清除“结果待补”                                                                                             | 上传后可清待办，软删除后待办恢复                     |
| M-17 | 基础资料权限与会议结论冲突                                                 | MaterialLot、Setup、Instrument 的创建、追加版本和附件维护仅管理员可写；成员只读和引用                                                                 | 成员写入 403，管理员成功                             |
| M-18 | 炉次设备投影可被通用模块接口覆盖                                           | Setup 快照是唯一权威；设备投影只读，普通模块写入返回冲突                                                                                              | 注入冲突 payload 也不污染 R0/导出                    |
| M-19 | 实体版本载荷可接收任意字典和不适用字段                                     | 三类实体使用强类型载荷；未知字段与条件不适用字段拒绝                                                                                                  | OpenAPI 和 422 用例                                  |
| M-20 | R0 只检查“非空”，不同过程记录可拼成假合规                                  | R0 先按生成模型复验结构，并在同一适用记录内验证温度、气体、压力等组合                                                                                 | 分散残缺数据不能锁定                                 |
| M-21 | 结果、仪器、物料引用缺少前置筛选                                           | 下拉按类别/方法过滤；选中后带出只读快照；错误在当前字段显示，不等后端 422                                                                             | 浏览器交互验收                                       |
| M-22 | 字段字典与导出不足以重建数据                                               | 字典补输入类型、机器类型、说明、示例和 schema 路径；ZIP 带 JSON Schema；结构数组完整关系化                                                            | JSON/CSV ZIP 可追溯重建                              |
| M-23 | 页面把“可重复条目”、斜杠拼接和内部机器词暴露给用户；制备与表征没有并列入口 | 删除各章节灰色说明和机器化噪音；复合概念拆字段或改为递进文案；制备实验记录、表征实验记录并列一级导航，独立表征列表复用统一结果并可回到制备记录或样品  | 主导航与表征列表回归 + 用户可见文案扫描 + 浏览器截图 |
| M-24 | STATUS 把已知延期问题写成“全部完成”                                        | 更新真实状态、字段数量、门禁和剩余专业待确认项                                                                                                        | 文档与代码/测试一致                                  |

## 3. 会议未逐项点出、但代码审计发现的缺口

| 编号 | 问题                                                                 | 整改结果                                                                          | 验收证据                                               |
| ---- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------------ |
| A-01 | 气体下拉可引用错误种类/错误气体的批次，纯度快照形状不一致            | 气体种类、气瓶批次身份和纯度快照前后端双重校验；切换气体时清除不兼容引用          | 错气体批次保存 422；正确批次刷新后仍显示冻结版本与纯度 |
| A-02 | 温区数组只校验“编号不越界”，未保证覆盖装置全部温区                   | 设定温度与温度传感器均要求恰好覆盖 1…N 且不重复                                   | 缺温区、重复温区、越界温区分别失败                     |
| A-03 | 供气和外场时间可超过反应总时长                                       | 保存与 R0 同时校验各区间起止及总时长上界                                          | 超时区间前端拦截、后端 422                             |
| A-04 | “其他方法”“其他观察现象”可选中但没有具体内容，空壳仍被算作结果证据   | 增加具名明细字段；选择其他时明细必填；空 other 不清除结果待补                     | DTO、证据判定与表单回归                                |
| A-05 | 仪器列表不按方法筛选，改方法后可残留不兼容仪器                       | 按仪器方法和具名其他方法过滤；切换方法立即清除不兼容引用                          | Raman/光镜/其他方法正反用例                            |
| A-06 | 导出附件表缺少稳定文件键和绑定键，无法从 CSV 重建关系                | `files.csv` 增加 file_id、binding_type、binding_id，事件和温度附件均可回链        | ZIP 导出关系测试                                       |
| A-07 | 临时事件/温度文件上传后删条目或离开页面会形成不可见孤儿              | 后端保存时事务清理已移除绑定；前端跟踪本次上传，未保存即离开/移除时调用现有软删除 | 生命周期单测 + 浏览器网络检查                          |
| A-08 | 冻结引用可能被最新版本冒充；“其他”值下次仍只能重新输入               | 始终显示实际冻结版本/快照；把实体最新版本中的非空其他值并入同类下拉               | 旧版本引用和历史选项回归                               |
| A-09 | 旧草稿阶段无法映射时只表现为字段消失；结果附件数组为 null 时页面崩溃 | 旧结构明确要求删除或重录，不做猜测迁移；附件数组按空数组容错                      | 历史载荷提示 + null 回归                               |

## 4. 实施顺序

1. **字段与结构契约**：先改 `field-source.yaml` 和有限的具名结构类型，未知复杂输入类型生成时直接失败。
2. **后端可信边界**：生成模型、实体强类型、Setup 快照权威、附件角色、R0、权限和导出。
3. **前端录入体验**：复用现有结构化控件和附件链路，重做条件展开、排序、筛选和就地错误。
4. **标准与生成物**：重生成 Pydantic、JSON Schema、OpenAPI、TypeScript 元数据、字段字典和 xlsx。
5. **全门禁与真实浏览器**：完成一条炉次的创建、保存、刷新、锁定、样品、结果/附件和导出；检查中英双语与控制台。

## 5. 完成定义

- 后端 lint/format/pytest、前端 format/lint/typecheck/test/build、字段源与全部生成物漂移检查全绿。最终数字：后端 467 passed、4 skipped；前端 52 files、358 tests。
- 新旧结构不能混写；若本地存在旧草稿过程载荷，必须明确要求重填，不能猜测转换。
- 已用 `CVD-2026-0001` 完成两种气体、二次供气、两温区温度程序、倾斜衬底、过程事件附件、实测温度、直接观察附件和 Raman 原始谱的本地主线。
- 锁定后的 JSON 与 CSV ZIP 已核对原始值及文件关联；最终 `check-r0` 为 compliant，44 项运行时合规判据中 43 项适用且 43/43 通过。
- 最终报告已回填 M-01—M-24、A-01—A-09、F-01—F-12、自动化和浏览器证据；F-01—F-12 全部关闭，最终未闭环 P0/P1 = 0；11 项组内专业裁定单列，不混入“已完成”。
- PR #1 合并后，Actions `30139599899` 五项 required checks 全绿；香港生产按普通增量流程从 `a592116` 前滚到 `57a25b7`，发布前备份为 `backups/20260725_100220`，部署后健康、数据保持和真实浏览器只读复验通过。

## 6. 汇报时需要组内确认但不阻塞实现的 11 个专业项

1. “观察到的现象”词表粒度与稳定选项。
2. SEM “覆盖率”的组内标准叫法、量化方式和测量方法。
3. 堆垛类型需要区分到什么粒度。
4. 前驱体“外观”的稳定选项。
5. 中文界面采用“气路置换/洗气/吹扫”中的哪一个组内标准名称。
6. 低压 CVD 是否每炉次记录漏率，以及该参数属于装置校验还是实验过程。
7. 多气体比例的组内标准名称；系统先由同时间段各气体流量派生，不另存第二份比例。
8. 掺杂体系组成角色使用“本征材料/基体/主体”的最终术语，以及复杂官能团化学式的组内示例。
9. 压片等前驱体处理方式的最小参数模板；未确认参数通过具名“参数—值—单位”保存，不使用无单位自由文本。
10. 低压系统的真空泵类型、功率和转速分别属于 Setup 版本还是逐炉过程条件。
11. 物料是否必须做真实瓶装试录、目录号是否必填，以及 CoA 与瓶身/包装标签照片的最小证据边界。
