import { describe, it } from "vitest";

describe("FurnaceProgramSection", () => {
  it.todo("renders furnace info, precursors, and steps sections");
  it.todo("applies a built-in furnace template and clears the applied alert on manual edit");
  it.todo("updates zone temperatures when zonesCount changes");
  it.todo("shows matching recipe templates and applies their furnace payload");
});